version: '3'

services:
  app:
    image: docker-nextjs-dev
    build: .
    ports:
      - 8443:8212
    volumes:
      - ./pages:/app/pages
      - ./public:/app/public
      - ./styles:/app/styles
      - ./components:/app/components
